# Chỉ dẫn cho Claude

Bắt buộc đọc và tuân theo `AGENTS.md` trước khi sửa hoặc chạy lệnh. `AGENTS.md` là hợp đồng AI chuẩn; tiếp theo đọc `docs/PROJECT_MAP.md` và tài liệu đúng miền. Không tạo một bộ quy tắc song song trong file này.
